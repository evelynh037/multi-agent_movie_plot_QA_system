# ── Web framework ─────────────────────────────────────────────────────────────
flask>=3.0
python-dotenv>=1.0

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai>=1.30

# ── LangChain ecosystem ───────────────────────────────────────────────────────
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.1
langchain-pinecone>=0.1
langchain-text-splitters>=0.2
langsmith>=0.1
langgraph>=0.1

# ── Vector database ───────────────────────────────────────────────────────────
pinecone-client>=3.0

# ── Embeddings & reranking ────────────────────────────────────────────────────
# Pinned to avoid transformers 4.47+ bug on Python 3.10 (aten::isin MPS issue)
transformers==4.44.2
sentence-transformers==3.0.1
huggingface-hub>=0.23
tokenizers>=0.15.0

# ── LoRA fine-tuning (PEFT) ───────────────────────────────────────────────────
peft>=0.10.0
accelerate>=0.25.0

# ── Deep learning ─────────────────────────────────────────────────────────────
# For GPU: install PyTorch with CUDA from https://pytorch.org
torch>=2.1.0

# ── Data & ML utilities ───────────────────────────────────────────────────────
numpy<2.0          # pinned: numpy 2.x breaks torch .numpy() on older builds
pandas>=2.0.0
scikit-learn>=1.3.0
scipy
datasets>=2.16.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn
tqdm>=4.66.0

# ── Web scraping (WebBaseLoader) ──────────────────────────────────────────────
beautifulsoup4
requests